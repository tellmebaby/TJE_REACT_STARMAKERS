<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.aloha.server.mapper.PointMapper">

<!--  목록  -->
<select id="list" resultType=""> SELECT * FROM point </select>
<!--  조회  -->
<select id="select" resultType=""> SELECT * FROM point WHERE id = #{id} </select>
<!--  등록  -->
<insert id="insert"> INSERT INTO point ( ???, ??? ) VALUES ( #{???}, #{???} ) </insert>
<!--  수정  -->
<update id="update"> UPDATE point SET ??? = #{???} ,updated_at = now() WHERE id = #{id} </update>
<!--  삭제  -->
<delete id="delete"> DELETE FROM point WHERE id = #{id} </delete>
</mapper>